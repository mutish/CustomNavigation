<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavigationApp"
        tools:targetApi="31">
        <activity
            android:name=".tabscreen"
            android:exported="false"
            android:label="@string/title_activity_tabscreen"
            android:theme="@style/Theme.NavigationApp" />
        <activity
            android:name=".SideNavigationActivity"
            android:exported="false"
            android:label="@string/title_activity_side_navigation"
            android:theme="@style/Theme.NavigationApp" />
        <activity
            android:name=".BottomNavigationActivity"
            android:exported="true"
            android:label="@string/title_activity_bottom_navigation"
            android:theme="@style/Theme.NavigationApp"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.NavigationApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>